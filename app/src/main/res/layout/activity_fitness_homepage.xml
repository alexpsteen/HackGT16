<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_fitness_homepage"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.alexsteen.hackgt16.FitnessHomepage">

    <Button android:id = "@+id/ChecklistButton"
        android:layout_height="wrap_content"
        android:layout_width="wrap_content"
        android:onClick="goToChecklist"
        android:text = "Checklist"/>

    <Button android:id = "@+id/StepCountButton"
        android:layout_height="wrap_content"
        android:layout_width="wrap_content"
        android:onClick="goToStepCounter"
        android:layout_marginTop="56dp"
        android:text = "Step Count"/>

    <Button android:id="@+id/TimerButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:onClick="goToTimer"
        android:layout_below="@+id/ChecklistButton"
        android:layout_alignParentStart="true"
        android:layout_marginTop="62dp"
        android:text = "Timer"/>

</RelativeLayout>
